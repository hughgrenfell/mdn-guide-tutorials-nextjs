/** @type {import('next').NextConfig} */
const nextConfig = {
    /*
    // Remove this comment and surroned comments tags when ready to deploy!
    output: "export",
    */
}

module.exports = nextConfig
